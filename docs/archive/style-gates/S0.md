# Phase S0 — Token + theme reconciliation

Status: **PASS (pending QA + architect-review).**

Scope: §5 (token + theme baseline) and §10 Phase S0 of `airavata-custos/docs/portal/2026-05-22-nexus-portal-styling-alignment.md`.

## What landed

Commits since the Phase 7 closing commit `b654f9c`:

```
f347c6b Add token presence smoke test
68d344a Default theme to light with explicit nexus theme storage key
97c75db Repoint primary to near-black and expose brand semantic to Tailwind
8c4e99c Reconcile gray scale and add brand semantic tokens
```

## Token diff summary (before → after)

### Gray scale (`design-tokens/colors.css`)

| Token | Before | After |
|---|---|---|
| `--nexus-gray-50` | `#f4f5f7` | `#f4f5f7` (unchanged) |
| `--nexus-gray-100` | `#e5e7eb` | `#f1f1f1` (collaborator sidebar bg / `bg-muted`) |
| `--nexus-gray-200` | `#cbd0d8` | `#e5e7eb` (default border) |
| `--nexus-gray-300` | `#9aa1ae` | `#d4d4d4` (collaborator `border-neutral-300`) |
| `--nexus-gray-400` | `#6b7280` | `#9aa1ae` |
| `--nexus-gray-500` | `#515f74` | `#6b7280` (muted text) |
| `--nexus-gray-600` | `#3d4658` | `#4b5563` |
| `--nexus-gray-700` | `#2a3142` | `#404040` (secondary text) |
| `--nexus-gray-800` | `#1a1f2b` | `#262626` |
| `--nexus-gray-900` | `#191c1e` | `#111111` (primary text, collaborator body) |

Stale "derived around observed text colors" comment block removed.

### Primary semantic

| Token | Before | After |
|---|---|---|
| `--primary` (light) | `var(--nexus-blue-500)` (≈ `#2361b8`) | `oklch(0.205 0 0)` — near-black, matches collaborator + Figma "Add user" button |
| `--primary-foreground` (light) | `var(--nexus-white)` | `oklch(0.985 0 0)` — near-white |
| `--primary` (dark) | unchanged (`var(--nexus-blue-400)`) | unchanged |

Dark mode primary intentionally left as-is — only adjusted when token *shape* changes.

### Sidebar tokens (light)

| Token | Before | After |
|---|---|---|
| `--sidebar` | `var(--nexus-gray-50)` (`#f4f5f7`) | `#ffffff` — Figma sidebar is pure white |
| `--sidebar-accent` | `var(--nexus-white)` | `var(--brand-tint)` — active-item tint |
| `--sidebar-accent-foreground` | `var(--nexus-gray-900)` | `var(--brand)` — active-item label |

### Surface tokens (light)

| Token | Before | After |
|---|---|---|
| `--background` | `var(--nexus-white)` | `#ffffff` |
| `--card` | `var(--nexus-white)` | `#ffffff` |
| `--muted` | `var(--nexus-gray-100)` | `var(--nexus-gray-100)` (unchanged, value now `#f1f1f1`) |
| `--border` | `var(--nexus-gray-200)` | `var(--nexus-gray-200)` (unchanged, value now `#e5e7eb`) |
| `--input` | `var(--nexus-gray-200)` | `var(--nexus-gray-200)` (unchanged, value now `#e5e7eb`) |

### New brand semantic tokens

Added in `:root`:

```
--brand: var(--nexus-blue-500);
--brand-foreground: #ffffff;
--brand-tint: var(--nexus-blue-50);
```

Mirrored in `.dark`:

```
--brand: var(--nexus-blue-400);
--brand-foreground: #ffffff;
--brand-tint: rgba(35, 97, 184, 0.15);
```

Exposed to Tailwind via `@theme inline` in `src/app/globals.css` so utilities
`bg-brand`, `text-brand`, `bg-brand-tint`, `text-brand-foreground` are available.

### Theme defaults

| File | Before | After |
|---|---|---|
| `src/shared/providers/Providers.tsx` | `defaultTheme="system"`, `enableSystem` | `defaultTheme="light"`, `enableSystem={false}`, `storageKey="nexus.theme"` |
| `src/app/layout.tsx` `<html>` className | `cn("h-full antialiased", …)` | `cn("light h-full antialiased", …)` — first paint is light pre-hydration |

`prefers-color-scheme: dark` no longer auto-switches; dark mode remains accessible via Settings.

## Token smoke test

Added `src/shared/__tests__/tokens.test.ts` (Vitest). Reads `design-tokens/colors.css` with `node:fs` and asserts:

- `--nexus-gray-900: #111111` and `--nexus-gray-100: #f1f1f1` are present.
- `--brand: var(--nexus-blue-500)`, `--brand-foreground: #ffffff`, `--brand-tint: var(--nexus-blue-50)` are present in `:root`.
- The `.dark` block also defines `--brand` and `--brand-tint`.

Suite: **3 tests, all passing.**

## Definition of Done — Phase S0

- [x] Light mode is the default boot state (`<html class="light">` + `defaultTheme="light"` + `enableSystem={false}`).
- [x] No hardcoded brand utilities introduced in the tokens layer; brand semantic exposed through `var(--brand)`.
- [x] `pnpm verify` green: `Test Files 18 passed (18) · Tests 116 passed (116)`, lint + typecheck + build all clean.
- [x] Every commit on `main` is buildable (`pnpm build` run between every commit).
- [x] All work landed in `nexus-portal`; nothing committed to `airavata-custos`.

## Open items / deferrals

- **Component-level brand usage is deferred to Phase S2.** Existing `text-nexus-blue-*` and `bg-nexus-blue-*` utilities still litter `src/` (sidebar active state, NEXUS wordmark, links, focus states). Phase S4 sweep will replace them with `text-brand` / `bg-brand` / `bg-brand-tint`. Out of S0 scope per the contract — "Do NOT touch any code outside design-tokens/, src/app/globals.css, src/app/layout.tsx, src/shared/providers/Providers.tsx".
- **Existing dark-mode users.** No migration needed: `storageKey="nexus.theme"` is new, so anyone who previously set the OS-driven `system` mode will boot light on first load and can re-opt-in via Settings. Spec accepts this since Settings already exposes the toggle (Phase 7).
- **Primary button consumers.** Buttons using `bg-primary` are now near-black instead of blue. Visual regression is intended (Figma + collaborator alignment), but Phase S2 will verify each consumer (CTA buttons, sidebar `Get Support`, drawer footers, etc.) still reads correctly.
- **Dark mode primary.** Kept as blue (`--nexus-blue-400`) per the contract's "only adjust if a token changes shape" guard. If QA wants dark-mode CTAs to also be near-black for consistency, raise it in S0 review and we'll land a follow-up.

## Sign-off

- QA (visual + boot behavior, light mode default at 1440×900, no flicker against OS dark): _pending_
- Architect review (token discipline, no brand utilities outside tokens layer, brand vs primary boundary): _pending_

## Architect-review (PASS)

Brand-vs-primary semantic boundary correctly drawn at the token layer. Diff envelope tight (6 files exactly, no component churn). Light-boot path is canonical: `<html className="light">` + `defaultTheme="light"` + `enableSystem={false}` + `storageKey="nexus.theme"`. All five commits buildable per `git bisect`-friendly history.

### LOW (deferred)
1. Dark `--brand-tint` uses `rgba(35,97,184,0.15)` literal — bypasses palette ref. Future: `color-mix(in srgb, var(--nexus-blue-400) 15%, transparent)`.
2. Token smoke test asserts brand/primary substring presence but not exact OKLCH values — tighten in S1 or S2 (5-line change).
3. Spec §5.2 says `--muted` light = `#F4F5F7` but implementation uses `#f1f1f1` (collaborator's sidebar bg). Either is defensible; pick one and amend spec.
4. Stale palette tokens (e.g., orphaned amber/red/green steps) — audit in S4 sweep.

Sign-off: PASS. S1 may proceed.

## Visual QA (PASS-WITH-NOTES)

**Token resolution verified at runtime:**
- `body bg = rgb(255, 255, 255)`, `--primary = oklch(0.205 0 0)`, `--brand = #2361b8`, `--background = #ffffff`
- `bg-brand` utility → `rgb(35, 97, 184)`; `bg-brand-tint` → `rgb(233, 240, 248)`; `text-brand` → `rgb(35, 97, 184)`
- `/sign-in` Continue button: `bg = oklch(0.205 0 0)`, `color = oklch(0.985 0 0)` (near-black, white text)
- `<html className=... light>` set inline; no light→dark flicker on first load

**Notes (non-blocking):**
- NEXUS wordmark on `/sign-in` still uses `text-nexus-blue-700` (`#153b6e`) instead of `text-brand` (`#2361b8`). Recategorize as S4 cleanup (hardcoded brand-utility sweep is exactly S4's scope).
- Dev server entered a stuck state after multiple reloads (turbopack incremental-compile glitch — unrelated to S0). Restart before S1 QA.
- `/settings` theme-toggle persistence not verified due to the dev-server hang; roll into S5 page-by-page conformance.

Sign-off: PASS-WITH-NOTES. S1 may proceed.
