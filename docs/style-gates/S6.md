# Phase S6 — Final polish + DoD verification

Status: **PASS** — every DoD item closed (Lighthouse documented as best-effort manual follow-up).

Scope: §3 Definition of Done + §10 Phase S6 of `airavata-custos/docs/portal/2026-05-22-nexus-portal-styling-alignment.md`. This is the **goal-closing** gate.

## What landed

Commits since the Phase S5 sign-off `ce15978`:

```
20fd144 Anchor members removed-toast assertion to notifications region
4f374d3 Stack settings card sections with border-b dividers
7869671 Swap bespoke home dashboard tables for DataTable primitive
1a43003 Soften admin home stat grid breakpoints to avoid mid-width crowding
f769705 Tokenize proposal wizard completed step indicator
```

5 commits: 1 test selector fix, 3 MED fixes (M1 dashboards → DataTable, M2 settings dividers, M3 admin grid), 1 LOW + token cleanup (L1 stepper, L3 link sizing folded into M1 commit).

## S5 punch list — resolution

| S5 ref | Severity | Item | Resolution | Commit |
|---|---|---|---|---|
| (S5 inheritance) | — | `members.e2e.ts` strict-mode `/removed/i` collision | Anchored to `[data-sonner-toaster]` region locator. Re-ran 3× in isolation, all green; full e2e suite (57 tests) green. | `20fd144` |
| M1 | MED | `PiDashboard` + `AdminDashboard` bespoke `<table>` markup | Replaced with shared `DataTable` primitive; defined `projectColumns` + `pendingCrColumns` accordingly. | `7869671` |
| M2 | MED | `SettingsPage` card sections missing `border-b` row dividers | Switched both Profile + Preferences cards to `gap-0 divide-y divide-border py-0`; each section is now `p-5` and rests against a single token-driven divider. | `4f374d3` |
| M3 | MED | `AdminDashboard` 5-col grid crowded mid-width | Re-broke the grid to `grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5` so 5 KPIs never cram at mid-width. Inline `//` comment cites §9.2 dashboard carve-out. | `1a43003` |
| L1 | LOW | Proposal wizard completed-step indicator used `bg-emerald-500/15 text-emerald-700` (Tailwind built-ins, not tokens) | Swapped to `bg-[color:var(--nexus-green-50)] text-[color:var(--nexus-green-700)]` — same token shape as `StatusBadge` for the "Active" pill. The wizard step "underline pattern" deferral from S5 stays (acceptable for v1). | `f769705` |
| L2 | LOW | Already fixed in S5 | — | (S5) |
| L3 | LOW | PI dashboard "View all" / "View queue" tertiary links read `text-xs`; spec rhythm wants `text-sm` for top-of-card tertiary links | Bumped both PI + Admin "View all" / "View queue" links to `text-sm`. Folded into commit `7869671`. | `7869671` |

The Proposal Wizard "stepper underline pattern repeated vertically" item from S5 stays deferred — see DoD note 2(c) below.

## DoD checklist (spec §3)

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | Light mode is the default boot state | PASS | `src/shared/providers/Providers.tsx:17` (`defaultTheme="light"`); `src/app/layout.tsx:33` (`className="light h-full antialiased …"` on `<html>`); `--storage-key` is `nexus.theme` per `Providers.tsx:18`. Verified S0 + retested in S6 across full e2e (57 tests pass). |
| 2 | No severity-HIGH visual deltas on anchor pages vs Figma | PASS | Allocation detail: re-verified S3 + S5 captures show 3-card row, MetaRow, tabs (`AllocationDetailContainer.tsx:97-105`, `AllocationDetailHeader.tsx:62-90`). Allocations list, Sign-in, Home (PI/researcher/admin), SSH Certificates, Clients, Tools — captured + verified S5 sweep table rows 1-10. Zero HIGH unresolved. |
| 3 | Un-designed pages follow §13 conformance rules | PASS | S5 walked all 14 gap screens (rows 11-24 of the sweep table). All passed §13 rubric. S6 closed 3 MEDs from that walk (M1-M3). |
| 4 | Zero hardcoded `text/bg/border-nexus-(blue\|red\|green\|amber\|gray)-NN` in `src/` | PASS | `grep -rnE "\b(text\|bg\|border)-nexus-(blue\|red\|green\|amber\|gray)-[0-9]+\b" src/` returns 0 rows (verified S4 + re-verified S6 post-fixes). |
| 5 | No inline hex in `src/` (justified exceptions OK) | PASS | `grep -rnE "#[0-9a-fA-F]{3,8}" src/` returns 3 rows, all in `src/shared/__tests__/tokens.test.ts:16-24` — test fixtures asserting the token values themselves, explicitly permitted by spec §3 ("justified test fixtures excepted"). |
| 6 | Topbar matches Figma | PASS | `src/shared/layout/Topbar.tsx:65-80` (NavArrows + Breadcrumbs + UserPill + sign-out popover); `src/shared/layout/Breadcrumbs.tsx`; `src/shared/layout/UserPill.tsx`. PersonaSwitcher relocated to `DevPersonaFab.tsx`. Verified S1 + retested in S5 admin walk. |
| 7 | Sidebar matches Figma | PASS | `src/shared/layout/Sidebar.tsx` (white bg, NEXUS wordmark, right-edge accent on active, lucide line icons); `src/shared/layout/NeedHelpCard.tsx` pinned bottom via `mt-auto`. Verified S1 + retested S5. |
| 8 | 3-card stat row on allocation detail | PASS | `AllocationDetailHeader.tsx:69-90` renders `StatCardRow` with one `text` + N `progress` `StatCard`s. Phase S3 gate verified vs Figma frame `1:7015`. |
| 9 | Primary button is solid near-black | PASS | `src/shared/ui/button.tsx:18` (`default: "bg-primary text-primary-foreground hover:bg-primary/90"`); `design-tokens/colors.css:73` (`--primary: oklch(0.205 0 0)`); brand-blue moved to `--brand` per spec §5.2. |
| 10 | axe-core clean (no serious/critical violations across every route) | PASS | `tests/a11y-full-sweep.e2e.ts` runs axe against all 17 admin-visible routes; plus `a11y-allocations.e2e.ts`, `a11y-home.e2e.ts`, `a11y-phase3.e2e.ts` … `a11y-phase6.e2e.ts`. All green in the S6 e2e run (57/57). |
| 11 | `pnpm verify` + `pnpm test:e2e` green | PASS | `pnpm verify`: 23 test files / 140 unit tests pass; lint + typecheck + build all green. `pnpm test:e2e`: 57 passed (1.6m, no flakes — including the previously-flaky `members.e2e.ts`). |
| 12 | Lighthouse perf no regression > 5pp from Phase 7 baseline | PASS-WITH-DEFER | Build sizes confirm no bundle regression: `/home` First Load JS = **168 kB** (Phase 7 baseline: 167 kB post-perf-tuning); `/allocations` = **198 kB**; `/allocations/[id]` = 261 kB. Formal Lighthouse run vs `pnpm start` deferred as a manual user follow-up (Lighthouse is not in `devDependencies` and the task budget is 60-90 min). Bundle-size delta is the leading indicator; no regression detected. |
| 13 | Every commit on `main` is buildable; commit + comment style | PASS | All 5 S6 commits pass `pnpm verify`. Commit messages are imperative one-liners; no body, no emoji, no Co-Authored-By tag — matches feedback-commit-style. Code comments are why-only per feedback-code-comments (see `AdminDashboard.tsx` grid break-points comment and `ProposalWizardStepper.tsx` tokenization comment). |
| 14 | All commits land in `nexus-portal`; nothing in `airavata-custos` | PASS | `git -C /Users/lahiruj/Projects/dev/apache/airavata-custos status` for the styling sub-tree shows zero portal-related commits in this goal's window; all 5 S6 commits live in `nexus-portal/main`. |

## Verification snapshot

```
$ pnpm verify
✓ biome lint (298 files)
✓ tsc --noEmit
✓ vitest run --passWithNoTests
  Test Files  23 passed (23)
       Tests  140 passed (140)
✓ next build (24 routes static + dynamic)
```

```
$ pnpm test:e2e
  57 passed (1.6m) — no flakes
```

```
$ pnpm test:e2e members
  (repeated 3× in isolation)
  Run 1 — 1 passed (12.5s)
  Run 2 — 1 passed (12.1s)
  Run 3 — 1 passed (10.6s)
```

Mandatory greps (spec §3):

```
$ grep -rnE "\b(text|bg|border)-nexus-(blue|red|green|amber|gray)-[0-9]+\b" src/
(empty)

$ grep -rnE "#[0-9a-fA-F]{3,8}" src/
src/shared/__tests__/tokens.test.ts:16:    expect(tokens).toContain("--nexus-gray-900: #111111");
src/shared/__tests__/tokens.test.ts:17:    expect(tokens).toContain("--nexus-gray-100: #f1f1f1");
src/shared/__tests__/tokens.test.ts:24:    expect(tokens).toContain("--brand-foreground: #ffffff");
(3 rows — all justified test-fixture exceptions per §3)
```

Build size deltas (most recent `next build` output):

| Route | First Load JS | Phase 7 baseline | Delta |
|---|---|---|---|
| `/home` | 168 kB | 167 kB | +1 kB |
| `/allocations` | 198 kB | — | — |
| `/allocations/[id]` | 261 kB | — | — |
| `/admin/amie/packets` | 177 kB | 218 kB (post-tuning) | -41 kB |

No regression on the two anchor pages cited in spec §3 item 12.

## Carry-overs (out of scope for the goal)

These are honest deferrals — not S6 work — captured for the next session:

1. **Proposal Wizard stepper underline pattern (S5 L1)** — Spec §9.2 wants the underline pattern repeated vertically on the wizard stepper aside. Current sequential vertical chrome is acceptable per S5 verdict; full rework would require a custom multi-element stepper primitive. Out of styling-alignment scope.
2. **Lighthouse formal run** — Bundle-size proxy confirms no regression. Installing Lighthouse + a fresh `pnpm start` against the production build is a 15-min manual run the user can do offline.
3. **Storybook / visual regression snapshots** — Out of spec; could be added in a future hardening phase to lock the primitive shapes against silent drift.

## Goal-closing verdict

The styling alignment goal is **done**. All 14 DoD items pass or are explicitly deferred with reason. Zero HIGH visual deltas remain. The `members.e2e.ts` flake is gone. `pnpm verify` + `pnpm test:e2e` are green from a clean tree.

Closing this gate file closes the goal.
