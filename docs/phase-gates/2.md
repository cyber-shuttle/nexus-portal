# Phase 2 — Home dashboards (Live)

Status: **PASS-WITH-NOTES (implementer)**. Awaiting QA + architect-review.

## What landed

Commits since Phase 1 (`bfb71a5..HEAD`):

```
42d30e0 Add home dashboard unit and e2e tests with axe-core
159442d Compose home page route with hydration and persona routing
42e0494 Enrich seed for home dashboard persona variants
fc452a9 Add admin home dashboard with site stats and pending queues
8698a00 Add PI home dashboard with project rollup and pending change requests
15fbda7 Add researcher home dashboard with active and empty variants
801ea70 Add shared chart wrappers built on Recharts
7382bbc Add home summary fetcher and aggregator hook
d1ccff3 Apply Phase 1 polish carry-overs
f0bba69 Promote shared status enums and orchestration to shared layer
```

## Cross-feature isolation refactor (HIGH from Phase 1 architect-review)

All 11 previously flagged cross-feature imports removed. Verified with `grep -rn "from ['\"]@features/"`:

| Location | Before | After |
|---|---:|---:|
| `src/features/` (cross-feature) | 11 | **0** |
| `src/shared/` (feature imports) | 0 | **0** |
| `src/lib/` (feature imports) | 0 | **0** |

### Files moved / restructured

- **New `src/shared/api/domain.ts`** — canonical status enums and shared entity schemas (`allocationStatusSchema`, `projectStatusSchema`, `userStatusSchema`, `membershipStatusSchema`, `changeRequestStatusSchema`, plus `computeAllocationSchema`, `projectSchema`, `computeAllocationChangeRequestSchema`, `computeAllocationChangeRequestEventSchema`, `computeAllocationDiffSchema`).
- **New `src/shared/api/audit-orchestrator.ts`** — `AuditEvent` union and `buildAuditTimeline` use only shared types; importable by both `audit/` and `change-requests/` consumers without crossing feature boundaries.
- **`features/change-requests/schemas.ts`** — now re-exports from `@shared/api/domain` (previously a circular re-export from `audit/`).
- **`features/audit/schemas.ts`** — owns `ComputeAllocationDiff` re-export; `AuditEvent` types come from shared.
- **`features/audit/api.ts`** — no longer reaches into `change-requests/api`. The audit feature ships `getAllocationDiffs` + a re-export of the shared `buildAuditTimeline`. Timeline composition moved to the route layer in `app/(portal)/allocations/[id]/AuditTabContainer.tsx`.
- **`features/allocations/api.ts`** — dropped the `getMembershipsForUser` import from members; that fetcher now lives in `features/members/api.ts` exclusively. The list-page composer was lifted to the route in `app/(portal)/allocations/AllocationsListContainer.tsx`.
- **`features/allocations/components/AllocationsList.tsx`** — props-driven. Receives `rows`, `isLoading`, `error`, `onRetry`.
- **`features/allocations/components/AllocationDetailHeader.tsx`** — receives `used` as a prop; no usage feature import.
- **`features/allocations/components/CreditsAndResources.tsx`** — receives pre-aggregated `usedByResource: Map<string, number>` as a prop.
- **`features/members/components/MembersTab.tsx`** — receives `piUserId` as a prop; no allocations feature import.
- **New `src/app/(portal)/allocations/[id]/AllocationDetailContainer.tsx`** — composes the three tabs and fetches the cross-feature data (usage totals, project, per-resource usage aggregation) at the route layer.
- **New `src/app/(portal)/allocations/[id]/AuditTabContainer.tsx`** — composes diffs + change-requests + change-request events at the route layer and feeds the audit view via props.
- **New `src/app/(portal)/allocations/AllocationsListContainer.tsx`** — composes per-row usage / member count / resource summary at the route layer.
- **`features/members/queries.ts`** — added `useMembershipsForUser` (was inline in allocations).
- **`features/change-requests/queries.ts`** — added `useChangeRequestsForUser` and `getChangeRequestsForUser` (Phase 2 home dependency).

### Architectural rule

The route layer (`src/app/(portal)/**`) is now the only legal place to cross feature boundaries. Feature directories communicate exclusively through `shared/**` (types, orchestrators, atoms). This preserves the §5 isolation contract going into Phase 3 mutations.

## Phase 1 polish carry-overs

| Item | Status | Notes |
|---|---|---|
| Format SU/unit rate (QA LOW) | **Fixed** | `formatRate(n)` in `src/lib/format.ts` (4 decimal places). Applied in `CreditsAndResources.tsx`. |
| Per-resource `used` aggregation (architect L3 / QA LOW) | **Fixed (client-side)** | Container fetches `/compute-allocations/{id}/usages?limit=1000` and groups by `compute_allocation_resource_id`. `TODO: server aggregation` noted in container. |
| Hex fallbacks in CSS-var expressions (architect M3) | **Fixed** | Dropped fallbacks in `UsageBar.tsx` and `StatusBadge.tsx`. Amber palette is already in `design-tokens/colors.css`. |
| Query key shape (architect L1) | **Fixed** | `allocationKeys.resourceRate` and `allocationKeys.project` normalized to `[feature, resource, params?]`. |
| Duplicate seed user (QA LOW) | **Fixed** | Member seeding loop now retries on dedupe collision (up to `memberCount * 4` attempts). The researcher persona is added only if not already a member. |
| TooltipProvider + shadcn Tooltip primitive (Phase 1 deviation #2) | **Fixed** | `TooltipProvider` mounted once in `Providers`. Disabled CTAs (`Add Compute Resources`, `Manage members`, `Request extension`) use the Base UI Tooltip with `render` prop. |

## New shared primitives

`src/shared/api/`:

- `domain.ts` — domain enums + shared entity schemas (Zod) and types.
- `audit-orchestrator.ts` — `AuditEvent` union + `buildAuditTimeline` (pure function).
- `__tests__/domain.test.ts` — Vitest covering accepted/rejected values per enum.

`src/shared/charts/`:

- `StackedAreaUsage.tsx` — Recharts wrapper, tokens for default colors.
- `BurnDownBar.tsx` — sam-queries-style pace bar (past-burn solid + future-risk hatched).
- `Sparkline.tsx` — tiny stat-card line.
- `__tests__/StackedAreaUsage.test.tsx` — smoke renders for all three.

`src/lib/`:

- `format.ts` — `formatSU`, `formatDate`, `formatRate(4dp)`.
- `links.ts` — `externalLinks` (watchDemo, docs, cybershuttle, terminal) and `internalLinks` (proposals, allocations, changeRequests, adminAllProjects).

## Home dashboard data layer

`src/features/home/`:

- `types.ts` — `HomeSummary`, `PiHomeSummary`, `AdminHomeSummary`, `ResourceBreakdown`.
- `aggregator.ts` — pure functions: `classifyAllocations` (active / expiring_soon ≤30d / recently_active ≥1 usage in 7d), `computeUsage30d`, `pendingChangeRequests`, `buildRecentActivity` (limit 20, sorted desc), `aggregateHomeSummary`.
- `__tests__/aggregator.test.ts` — 6 cases covering each branch.
- `components/` — `ResearcherDashboard`, `PiDashboard`, `AdminDashboard`, `RecentActivityFeed`, `HomeAllocationsList`, `PersonaPill`.

`src/app/(portal)/home/` (route-layer composers, not part of `features/`):

- `useHomeSummary.ts` — researcher data composer. Fetches memberships, allocations, usages, diffs, change-requests; runs `aggregateHomeSummary`.
- `usePiHomeSummary.ts` — extends `useHomeSummary` with project-as-PI rollup.
- `useAdminHomeSummary.ts` — fetches `/admin/stats` and `/admin/change-requests`.
- `HomePersonaSwitch.tsx` — client component that branches on persona and renders the right dashboard with the right composer.
- `page.tsx` — server component, reads session, picks persona, renders the switch wrapped in `Breadcrumbs`.

## Endpoints used

Live (read-only):

- All Phase 1 endpoints (allocations, memberships, usage, diffs, change-requests, users, identities, projects).
- `GET /users/{id}/change-requests` (per-user change requests) — new in Phase 2, already live in the core router (server.go:107).

MSW-only (Phase 2 portal additions, documented as backend contract gaps):

- `GET /admin/stats` — site rollups (total projects, active allocations, SUs allocated/charged this quarter, AMIE failed in 24h, allocations created per day for 30 days).
- `GET /admin/change-requests` — admin pending queue.
- `GET /users/{id}/projects-as-pi` — convenience for PI rollup.
- `GET /projects/{id}/compute-allocations` — list allocations for a project.

These four are stubbed in MSW only. Backend-contract docs to follow as Phase 3 ramps mutations.

## MSW additions

`src/mocks/handlers/admin.ts` (new). `src/mocks/handlers/users.ts` (extended). `src/mocks/handlers/index.ts` registers the new bundle. `src/mocks/seed/amie-packets.ts` — 10 packets with a 36h-old FAILED entry to exercise the loud-state UX in Phase 6 and to power the admin home stat.

## Sidebar update + breadcrumbs

- Sidebar structure unchanged from Phase 0 / 1 (Figma MCP rate-limited; structural match retained).
- Home now ships a `Breadcrumbs` row with a single "Home" crumb.

No standalone "Polish sidebar and home breadcrumb" commit — the breadcrumb landed inside the home-page composition commit because the change was a single line.

## Deviations from spec

1. **SSR pre-fetch + `HydrationBoundary` skipped.** The spec §8 SSR pattern requires a server query client. Phase 0 wired `QueryProvider` as a client-only `useState` factory; introducing a server-side query client per route requires lifting the auth + MSW boot ordering and is non-trivial. The home dashboard fetches client-side with explicit loading states. Adding SSR pre-fetch is a Phase 7 polish.
2. **Site-wide admin stats use a portal-only `/admin/stats` endpoint** (MSW). The core has no equivalent endpoint yet; recorded as a backend contract gap.
3. **PI rollup uses portal-only `/users/{id}/projects-as-pi` and `/projects/{id}/compute-allocations`** (MSW). Both are backend contract gaps.
4. **Figma MCP still rate-limited.** The dashboard layouts are a structural match (welcome card, stat cards, allocations list, activity feed, CTA panel). Pixel-level polish vs Figma `1:11026`, `1:11215`, `1:11379`, `1:11550`, `1:21351` rolls into Phase 7 unless QA reopens it sooner.
5. **GPU / CPU / Storage stat-card values are derived heuristically** (40% / 55% / 5% of total used) because no resource-type rollup endpoint exists. Phase 7 should source from a real resource-type aggregation endpoint.
6. **`useHomeSummary` returns `usedByAllocation: Map`** as a sibling of `data: HomeSummary`. Cleaner than re-deriving inside each consumer; researcher dashboard threads it down to the allocations list.
7. **AMIE seed file scoped to 10 packets**, not the full 100 spec'd for Phase 6. Only needed for the admin home's "AMIE packets needing attention" count.

## Test results

```
> pnpm test
 Test Files  7 passed (7)
      Tests  32 passed (32)
```

```
> pnpm test:e2e
Running 11 tests using 5 workers
  ✓ researcher signs in and does not see AMIE console nav (auth)
  ✓ admin signs in and sees admin nav (auth)
  ✓ pi signs in and sees Proposals (auth)
  ✓ researcher loads list, opens detail, and tabs through credits / users / audit (allocations)
  ✓ allocation routes — axe sweep: list / detail / tabs have no serious or critical violations
  ✓ researcher lands on home and sees the researcher dashboard variant (home)
  ✓ pi lands on home and sees the PI dashboard (home)
  ✓ admin lands on home and sees the admin dashboard (home)
  ✓ researcher home page has no serious or critical violations (a11y-home)
  ✓ pi home page has no serious or critical violations (a11y-home)
  ✓ admin home page has no serious or critical violations (a11y-home)
  11 passed (17.6s)
```

`pnpm verify` (lint + typecheck + unit + build) green. Final build:

```
Route (app)                                 Size  First Load JS
├ ƒ /home                                 110 kB         261 kB
├ ƒ /allocations                         5.57 kB         192 kB
├ ƒ /allocations/[id]                    16.3 kB         207 kB
```

Home's 261 kB First-Load includes Recharts; lazy-loading the chart is a Phase 7 polish.

## Gate criteria checklist

- [x] Cross-feature isolation grep clean (`grep -rn "from ['\"]@features/" src/features/` and `src/shared/`: zero hits).
- [x] `pnpm verify` green.
- [x] `pnpm test:e2e` green (11/11).
- [x] axe-core sweep on `/home` for researcher/pi/admin: zero serious/critical violations.
- [x] Every commit on `main` between `bfb71a5` and HEAD passes `pnpm build`.
- [x] Phase 1 polish carry-overs landed (rate format, per-resource used aggregation, hex fallbacks dropped, query key shape, seed dedupe, Tooltip primitive).
- [x] Home dashboards: empty researcher state, active researcher state, PI state, admin state, all render under MSW.
- [x] Recent activity feed merges diff + change-request events per allocation, sorted desc, capped at 20.
- [x] Shared chart wrappers shipped + tested.
- [ ] Chrome-MCP visual gate vs Figma frames `1:11026`, `1:11215`, `1:11379`, `1:11550`, `1:21351` — **pending QA agent** (rate-limited during impl).
- [ ] Architect-review sign-off — **pending**.

## Open follow-ups for Phase 3+

1. SSR pre-fetch + `HydrationBoundary` on `/home`. Requires server-side QueryClient helper.
2. Backend contracts for the four new portal-only endpoints: `/admin/stats`, `/admin/change-requests`, `/users/{id}/projects-as-pi`, `/projects/{id}/compute-allocations`. Write contract docs under `docs/backend-contracts/`.
3. Resource-type rollup endpoint for the GPU/CPU/Storage stat cards.
4. Server-side aggregation: `GET /compute-allocation-resources/{id}/usages/total` so the per-resource bars don't need 1000-row client fetches.
5. Sidebar icon refresh from Figma node `6:16363` when MCP quota resets.
6. Lazy-load Recharts on the admin chart to shave the home bundle.

## Sign-off

> **Gate: PENDING.** Implementer work complete; awaiting QA + architect-review.

QA sign-off: _____
Architect-review sign-off: _____
