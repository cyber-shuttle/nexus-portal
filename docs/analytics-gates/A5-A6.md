# Phase A5+A6 Gate — Backend-contract docs, polish, and DoD closure

**Spec:** `docs/portal/2026-05-22-nexus-portal-analytics.md` §3 DoD, §9 A5+A6.
**Baseline commit:** `a5fe1b0 Sign off Phase A4 gate with QA results`.
**HEAD commit:** `5f493bf Add /analytics to a11y full-sweep`.

This is the closing phase. A4 left two carry-overs (F1, F6) plus four A1
LOW polish items plus two A6 sundries. All are addressed here. After this
gate the analytics goal closes.

## Commits in A5+A6

```
657c95f Document analytics backend contracts comprehensively
d37dcc2 Add clean script for cache-flake workaround
d8b10c7 Render per-resource rows in admin at-risk table
6e5ac82 Improve sparkline a11y across KPI cards
3b0ca2d Verify LastSyncedBadge amber tint and DateRangePicker URL contract
ed82f0c Press only the saved view that matches current URL state
5f493bf Add /analytics to a11y full-sweep
```

(7 commits. The 8th suggested commit — this gate report — lands separately
once the verdict is final.)

## A4 carry-overs status

| # | Item | Status | Evidence |
|---|---|---|---|
| F1 | `pnpm verify` cache race on `next build` | DONE — workaround scripted | `d37dcc2`. New `pnpm clean` script (`rm -rf .next dist coverage`) documented in `scripts/README.md` as the workaround. Deliberately **not** chained into `pnpm verify` because the flake is intermittent and adding it would slow every local run. Run ad-hoc when verify trips on a page-collection error that the change set doesn't explain. |
| F6 | At-risk row resource granularity | DONE | `d8b10c7`. `AdminAnalyticsContainer` now enumerates (allocation × resource) pairs from the per-usage `compute_allocation_resource_id` stream. Per-resource capacity = `allocation.initial_su_amount / number_of_used_resources_in_allocation` (same heuristic as the compliance matrix). Per-resource forecast runs `forecast()` against a synthetic allocation with the split budget. Table caps at 50 rows (most urgent first) via the new `AT_RISK_ROW_LIMIT`; documented in `docs/backend-contracts/analytics.md` §5. `resourceName` resolves through `resourceRows` from `/admin/resources`. Row key updated to `${allocationId}::${resourceId}` so the new rows render uniquely. |
| LOW-1 | Sparkline a11y on every KpiCard | DONE | `6e5ac82`. `KpiCard` now derives a per-card sparkline `aria-label` from the card title (`"Burn rate trend sparkline"`, etc.) so every KPI surfaces a distinct sparkline in the a11y tree, not just the first one. New unit test asserts the labelled `<img role>` is queryable per card. |
| LOW-2 | LastSyncedBadge amber tint verification | DONE | `3b0ca2d`. The existing test asserted `data-tint="stale"` for between-1h-and-24h; refined to also assert the `--nexus-amber-50` + `--nexus-amber-700` semantic token classes are present so a stylesheet refactor that drops them fails here. Added a boundary test (`syncedAt = now − 1h − 1ms` flips to stale). |
| LOW-3 | DateRangePicker `?preset=` URL contract | DONE | `3b0ca2d`. Added two round-trip tests in `useUrlRange.test.tsx`: (1) preset chip — setRange writes `?preset=30d` only, no from/to; re-reading the URL resolves to the same preset + window. (2) custom range — setRange writes `?preset=custom&from=&to=`; re-reading resolves to identical ISO endpoints. The picker itself fires `onChange` (already covered); the hook is the URL contract gate. |
| A6-1 | Chip-pressed when state diverged | DONE | `ed82f0c`. `useSavedViewsToolbar` now derives the active chip id from URL state via the new `activeSavedViewId()` helper. The helper compares each view's snapshot to current `searchParams` + range + groupBy: preset name match for preset views, ISO endpoints for custom, and positional group-by match with trailing-`all` normalization so a 2-slot view still matches a 3-slot page. The moment the user tweaks the range or a group-by chip after applying, the press goes away. Removed the previous `useState<activeId>` that held the value through subsequent URL edits. New unit test covers preset+gb match, gb drift, range drift, trailing-`all` normalization, and custom-range matching. |
| A6-2 | Site-wide aggregation backend ask | DONE | `657c95f`. Documented in `docs/backend-contracts/analytics.md` §7 as the "Site-wide aggregation (backend ask, not in v1)" note, citing the PI member-contribution fan-out cost (N calls per project's memberships) and the proposed `/compute-allocations/{id}/members/usage-rollup?from&to` collapse endpoint. |

## A5 deliverables status

| Deliverable | Status | Evidence |
|---|---|---|
| `docs/backend-contracts/analytics.md` covers every MSW endpoint analytics touches | DONE | `657c95f`. The doc grew from 152 lines to 322 lines and now has 7 sections: (1) Researcher + PI `/jobs` + `/queues/wait-time`. (2) Admin enumerations `/admin/projects-full` + `/admin/allocations-full`. (3) Admin per-allocation usage fan-out — and the **proposed** `/admin/compute-allocation-usages/site-total`. (4) Per-allocation forecast — client-side method documented as the wire contract, plus the proposed `GET /compute-allocation-usages/{id}/forecast`. (5) Proposed `/admin/resource-mappings` / `/admin/resource-budgets` for the per-resource budget. (6) Saved views `/api/v1/analytics/views` GET/POST/PUT/DELETE with the full 409 status-code table. (7) Site-wide aggregation backend ask. Each section documents method, path, params, response shape, status codes, auth model, and "Known approximations" with explicit notes on what's heuristic in v1. |
| Cross-feature isolation greps stay zero | PASS | `grep -rn "from ['\"]@features/" src/features/` — only the pre-existing same-feature self-reference at `AllocationDetailHeader.tsx:7` (allocations → its own schemas, no boundary cross). `grep -rn "from ['\"]@features/" src/shared/` — 0. `grep -rnE "from ['\"](@features\|@shared)/" src/lib/` — 0. `grep -rn "from ['\"]\\.\\./[^'\"]*features/" src/features/` — 0. |
| Hardcoded utility greps stay zero | PASS | `grep -rnE "\\b(text\|bg\|border)-nexus-(blue\|red\|green\|amber\|gray)-[0-9]+\\b" src/` — 0 hits. Semantic tokens only. |

## A6 deliverables status

| Deliverable | Status | Evidence |
|---|---|---|
| `pnpm clean` script + docs for `next build` cache race | DONE | `d37dcc2`. See F1 above. |
| `/analytics` added to a11y full-sweep route list | DONE | `5f493bf`. `tests/a11y-full-sweep.e2e.ts` now sweeps 18 admin routes including `/analytics`. Full-sweep result below. |
| axe-core full sweep clean across analytics pages | PASS | 4 axe e2e tests pass: `a11y-analytics-admin.e2e.ts`, `a11y-analytics-pi.e2e.ts`, `a11y-analytics-researcher.e2e.ts`, and the new `/analytics` slot in `a11y-full-sweep.e2e.ts`. Zero serious/critical violations on any of the 3 persona variants. |
| Lighthouse perf snapshot per persona | DEFERRED — best-effort (per A6 spec) | Lighthouse CLI is not installed in the env and the spec said "best-effort, don't block." As a proxy, build-size for `/analytics` is **44.8 kB route + 388 kB first-load JS** (page-collection cache clean). This is the heaviest analytics route in the portal (Recharts + 7 primitives + 4 chart cards), but still under the 500 kB sidebar-cited budget. The persona breakouts share the same route (`/analytics` resolves variants at runtime), so the bundle is one shared blob, not three. |
| Final DoD §3 verification | See below | Walked every item. |

## Spec §3 DoD checklist

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | `/analytics` exists with 3 persona variants resolved server-side from session | PASS | `src/app/(portal)/analytics/page.tsx` calls `personaForAnalytics(session)` and routes to `AnalyticsPersonaSwitch`. A1/A2/A3 gates verified the resolution; auth e2e (`auth.e2e.ts`) verifies each persona lands on the right variant. |
| 2 | Each persona page renders ≥ 5 visualizations + a bottom table | PASS | Researcher (§6.1): KpiStrip + ForecastBar + StackedAreaUsage + WaitTimeBars + ResourceMixDonut + recent-jobs DataTable. PI (§6.2): KpiStrip + Burn-by-project ForecastBars + StackedAreaUsage + TopN member bars + ResourceMixDonut + Projects table. Admin (§6.3): KpiStrip + StackedAreaUsage cluster util + TopN top projects + StackedAreaUsage AMIE flow + ComplianceMatrix + at-risk DataTable. |
| 3 | All 7 new shared primitives ship + unit-tested | PASS | `KpiCard`, `DateRangePicker`, `GroupByChip(Group)`, `LastSyncedBadge`, `ForecastBar`, `ComplianceMatrix`, `DrillStack` — all in `src/shared/ui/` or `src/shared/charts/`; each has a `__tests__/*.test.tsx`. 274 unit tests pass total (41 files). |
| 4 | Date-range toolbar drives all charts via URL state, no per-card refetch | PASS | `useUrlRange` is the single source; every persona container reads `range` once and forwards to all chart props. Verified by `useUrlRange.test.tsx` round-trip + container code review. |
| 5 | Group-by chip changes propagate via URL state | PASS | `useUrlGroupBy` with `applyGroupByToParams`. Each container reads `groupBy[i]` slot and forwards to the relevant chart props. |
| 6 | Every widget shows "Last synced" timestamp | PASS | `LastSyncedBadge` is part of the toolbar `MetaRow` in every persona container. `syncedAt` resolves to the newest `last_updated` across the page's data sources. |
| 7 | Saved views: create/apply/delete via MSW, chips persist across reload | PASS | A4 gate verified. `analytics-saved-views.e2e.ts` covers the full round-trip incl. reload. |
| 8 | DrillStack drawer on chart segment click with breadcrumb | PASS | `DrillStack` ships with `crumbs`/`title`/`open`/`onClose`/`children`; persona pages wire it to chart segment clicks. Verified in PI + researcher e2e (`analytics-pi.e2e.ts`, `analytics-researcher.e2e.ts`). |
| 9 | CASL: `read Analytics<Persona>` enforced at route container | PASS | `AnalyticsPersonaSwitch` early-returns `ErrorState heading="Not permitted"` when the persona's ability check fails. Each `*AnalyticsContainer` re-checks `ability.can('read'/'manage', 'Analytics*')` as defense-in-depth. |
| 10 | Cross-feature isolation greps zero. Hardcoded-utility greps zero | PASS | See A5 row above. |
| 11 | `pnpm verify` + `pnpm test:e2e --workers=1` green | PASS | See verification output below. |
| 12 | axe-core sweep clean | PASS | See A6 row above. |
| 13 | Backend-contract docs published for every MSW endpoint | PASS | `docs/backend-contracts/analytics.md` covers all 7 endpoint families analytics touches. See A5 row above. |
| 14 | Every commit on `main` buildable; commit + comment style follows memory rules | PASS | Plain one-liner commits; why-only comments. Every commit was pushed only after `pnpm build` clean. |
| 15 | All commits in `nexus-portal`, none in `airavata-custos` | PASS | `git -C airavata-custos status --short` shows no `nexus-portal/*` paths and no commits since A4 sign-off. All A5+A6 commits live in `/Users/lahiruj/Projects/dev/apache/nexus-portal`. |

## Verification commands + output

```bash
# Unit tests
pnpm test                                  # 41 files, 274 tests pass
                                           # (267 pre-A5+A6 + 1 KpiCard a11y +
                                           #  2 LastSyncedBadge + 2 useUrlRange round-trip
                                           #  + 5 active-saved-view minus 3 already passing)

# Typecheck + lint
pnpm typecheck                             # clean
pnpm lint                                  # clean (360 files)

# Full clean build
pnpm clean && pnpm build                   # clean — /analytics route 44.8 kB / 388 kB
pnpm clean && pnpm verify                  # green end-to-end

# E2E (workers=1 per project convention)
pnpm test:e2e --workers=1                  # 68 passed (3.8m)
                                           # Includes new `/analytics` slot in full-sweep

# Isolation greps (all must be zero or only same-feature self-refs)
grep -rn "from ['\"]@features/" src/features/   # only AllocationDetailHeader.tsx:7 self-ref
grep -rn "from ['\"]@features/" src/shared/     # 0
grep -rnE "from ['\"](@features|@shared)/" src/lib/   # 0
grep -rn "from ['\"]\\.\\./[^'\"]*features/" src/features/   # 0

# Hardcoded utility greps
grep -rnE "\\b(text|bg|border)-nexus-(blue|red|green|amber|gray)-[0-9]+\\b" src/   # 0
```

## Open items (non-blocking)

- **A2 N6** — Avg burn /project label says `/mo` but value is window total. Cosmetic; out of A5+A6 scope.
- **A2 N7** — Member-bar Y-axis renders raw `user_id`. Out of A5+A6 scope; needs a user-display-name lookup.
- **A2 N2** — Multi-chip URL contract is page-local; saved-view payload captures literal slot array so cross-persona apply isn't safe. Documented in `analytics.md` §6 as the cross-persona apply caveat; a v2 `version` field + slot-name mapping unlocks it.
- **Saved-view range snapshot is fixed at save time.** Documented in `analytics.md` §6. "Update view to current URL state" affordance is a v2 follow-up.
- **Lighthouse perf snapshot.** Deferred to a follow-up tooling pass (Lighthouse CLI not installed in this env); bundle-size proxy noted above.
- **Long-press affordance on touch.** Not shipped; the trailing kebab is the discoverable option for delete/set-default. Long-press would be additive (post-A6).

## Goal closure verdict

**Y — closed.** Every DoD item in spec §3 passes. All A4 carry-overs landed.
All A5+A6 deliverables landed. `pnpm verify` + `pnpm test:e2e --workers=1`
both green. Cross-feature isolation greps zero. Hardcoded-utility greps
zero. All commits live in `nexus-portal`.

## Sign-off

- QA visual review: deferred to next visual sweep; functional + a11y are green.
- Architect review: deferred; the carry-overs that needed architect input (F3, F4, F5) closed in A4.

Sign-off: PASS. Analytics goal closed.
