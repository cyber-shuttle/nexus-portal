# Phase A1 Gate — Researcher `/analytics`

**Spec:** `docs/portal/2026-05-22-nexus-portal-analytics.md` §9 Phase A1 plus §6.1 layout, §3 DoD.
**Baseline commit:** `9cbb6a1 Sign off Phase A0 gate with architect results`.
**HEAD commit:** `3d5454e Keep analytics aggregations and tests free of cross-feature imports`.

## Commits in A1

```
147d0d0 Add explicit manage Analytics rule for admin
ed6a279 Document useUrlGroupBy contract for page-vs-chip use
eb05d8f Abbreviate LastSyncedBadge relative-time labels
33b16f4 Add insufficient-data flag to forecast result
ac3979d Add analytics route with persona-resolved entry point
475ae30 Add ResearcherAnalytics page with live wiring and MSW fallbacks
94486ca Gate analytics PI persona on explicit pi role
d0de066 Add Phase A1 unit and e2e tests with axe-core
3d5454e Keep analytics aggregations and tests free of cross-feature imports
```

## A0 carry-overs status

| # | Item | Evidence |
|---|---|---|
| 0a | Explicit `can('manage','Analytics')` for admin | `src/shared/casl/abilities.ts:64-66`; assertion at `src/shared/casl/__tests__/abilities.test.ts:74-77` |
| 0b | JSDoc on `useUrlGroupBy` | `src/shared/hooks/useUrlGroupBy.ts:6-10` |
| 0c | LastSyncedBadge "12m ago" abbreviation | `src/shared/ui/LastSyncedBadge.tsx:24-31` (formatter), `src/shared/ui/LastSyncedBadge.tsx:60` (call); tests at `src/shared/ui/__tests__/LastSyncedBadge.test.tsx:9-12, 14-21` |
| 0d | `forecast()` `insufficient-data` flag | `src/shared/api/aggregator.ts:194-241` (type + branches); tests at `src/shared/api/__tests__/aggregator.test.ts:197-237` |

## ResearcherAnalytics page composition

Route shell: `src/app/(portal)/analytics/page.tsx` — server component, reads session,
delegates to `personaForAnalytics(session)`, mounts `<AnalyticsPersonaSwitch persona userId>`.

Persona switch: `src/app/(portal)/analytics/AnalyticsPersonaSwitch.tsx` — client
component, routes `'researcher' → <ResearcherAnalyticsContainer userId />`,
`'pi' → <PlaceholderPage phase={2} />`, `'admin' → <PlaceholderPage phase={3} />`.

Route-layer fan-out: `src/app/(portal)/analytics/ResearcherAnalyticsContainer.tsx`
composes:

- `useMembershipsForUser(userId)` → user's allocation IDs.
- `useQueries(getAllocation)` per allocation.
- `useQueries(getAllocationUsages)` per allocation, scoped to URL range.
- `useJobs({ user_id, from, to, limit: 25 })` and `useQueueWaitTimes({ from, to })`.
- Picks `primaryAllocation` = highest-SU active allocation; runs
  `forecast(primaryUsages, primaryAllocation, range.to.getTime())`.
- Derives KPIs via `researcherKpisFromUsage`, daily stack via
  `bucketUsageByDayResource`, donut via `resourceMix`.
- Auth gate: early-returns `<ErrorState heading="Not permitted" />` when
  `ability.can('read', subject('AnalyticsResearcher', { userId }))` is false.

Presentational surface (`src/features/analytics/components/`):

- `ResearcherAnalytics.tsx` — H1 + viewing-as pill, MetaRow toolbar
  (`LastSyncedBadge` + `DateRangePicker` + `GroupByChip` + disabled `+ Save view`
  button), 5-up KPI strip, ForecastBar card with exhaust caption, daily
  StackedAreaUsage card (clickable → DrillStack), two-up Wait time +
  Resource mix, recent jobs DataTable, single DrillStack drawer.
- `AnalyticsCard.tsx` — inline §8 card chrome (`rounded-lg border bg-card p-5`).
- `WaitTimeBars.tsx`, `ResourceMixDonut.tsx` — Recharts wrappers using
  semantic token colors.

URL state: `useUrlRange` and `useUrlGroupBy` from `src/shared/hooks`. Range
defaults to last 30 days; group-by defaults to `resource`. Every card reads
the same range — no per-card refetch.

Nav: `src/shared/layout/navConfig.ts:32-39` adds the Analytics entry at slot 2
between Overview and Allocations, gated on `read AnalyticsResearcher`. Every
authed persona sees the link (admin via `manage all`, PI/researcher via their
own subject rule).

## MSW endpoints added

| Endpoint | Handler | Seed source |
|---|---|---|
| `GET /jobs?user_id&from&to&limit` | `src/mocks/handlers/jobs.ts:7-30` | `seed.jobs` — 200 seeded jobs; every 3rd job assigned to `researcher@nexus.local` so the demo table is populated. |
| `GET /queues/wait-time?from&to&group_by=queue` | `src/mocks/handlers/jobs.ts:33-37` | `seed.queueWaitTimes` — one row per of 5 queue names with `p50 ≤ p90`. |

Both payloads are Zod-validated in `src/features/analytics/schemas.ts` and the
test at `src/features/analytics/__tests__/jobs-handler.test.ts` invokes each
handler directly to assert the shape.

Backend contract published at `docs/backend-contracts/analytics.md` (resource
shapes, endpoint table, validation, auth, scope notes, open questions).

## DoD criteria — A1

- [x] `/analytics` route exists with persona-resolved variants — server-side
  `personaForAnalytics(session)`; researcher container live, PI/admin
  placeholders.
- [x] Researcher page renders ≥ 5 distinct visualizations: KPI strip (5
  cards), ForecastBar, StackedAreaUsage, WaitTimeBars, ResourceMixDonut +
  recent-jobs DataTable.
- [x] Date-range toolbar (24h/7d/30d/90d/custom) drives every chart via
  `?preset=&from=&to=` URL state.
- [x] Group-by chip propagates via `?gb=` URL state.
- [x] Last-synced badge present with refresh button. (Date computed from
  newest `last_updated` in fetched usages.)
- [x] DrillStack drawer opens on chart click with a breadcrumb back to
  the page; pop-back closes the drawer.
- [x] Auth gate: CASL `read AnalyticsResearcher` checked at the route
  container; "Not permitted" empty state on failure.
- [x] `pnpm verify` green (lint + typecheck + 213 unit tests + build).
- [x] `pnpm test:e2e --workers=1` green (59/59 incl. new analytics +
  a11y-analytics tests).
- [x] axe-core sweep on `/analytics` (researcher): zero serious/critical.
- [x] Backend-contract docs published (`docs/backend-contracts/analytics.md`).
- [x] Cross-feature isolation greps stay zero (only pre-existing
  `allocations → allocations` same-feature carry-over from S3).
- [x] Hardcoded brand-utility greps stay zero.
- [x] Every commit builds cleanly.
- [x] All commits land in `nexus-portal`; nothing committed to
  `airavata-custos`.

### Deferred to later phases

- **Saved views** — `+ Save view` button is rendered disabled per A4 plan.
- **Detail-link wiring** — Burn vs allocated card links to
  `/allocations/{id}?tab=credits`; the other cards omit "Detail →" because
  the linked pages don't exist yet.
- **DrillStack scope** — A1 wires one drilldown (daily chart → resource
  jobs). A2/A3 will widen to wait-time and resource-mix drills as their
  destinations clarify.

## Verification commands & output

```bash
pnpm verify   # lint + tsc + vitest 213/213 + next build → clean
pnpm test:e2e --workers=1   # 61/61 passed (incl. analytics-researcher + a11y-analytics-researcher)
grep -rn "from ['\"]@features/" src/features/   # only the pre-existing AllocationDetailHeader same-feature ref
grep -rn "from ['\"]@features/" src/shared/     # 0
grep -rnE "from ['\"](@features|@shared)/" src/lib/   # 0
grep -rn "from ['\"]\\.\\./[^'\"]*features/" src/features/   # 0
grep -rnE "\b(text|bg|border)-nexus-(blue|red|green|amber|gray)-[0-9]+\b" src/   # 0
```

## Open items / deferrals

- **Persona resolver gating change** — A0 shipped
  `personaForAnalytics` as "any PI membership → pi". The dev seed assigns
  `project_pi_id` from the first 8 users, which includes the researcher
  persona; that caused `loginAs('researcher')` to land on the PI placeholder
  in `/analytics`. Resolver now requires `role === 'pi'` explicitly (commit
  `94486ca`). Updated test asserts both the strict pi-role case and the
  "stale PI seed memberships do not promote a `user` role" guard. Tracks
  spec §5.4 first-bullet ("user / co_pi member-only → ResearcherAnalytics").
  Architect to confirm.
- **Detail-link coverage** — Only the Burn-vs-allocated card links to an
  existing destination. Daily / Wait time / Resource mix cards have no
  `Detail →` link in A1 because the underlying explorer surfaces don't
  exist. If the gate insists on a placeholder link to keep §8 chrome
  consistent, we can stub one in a fix-up commit.
- **MSW wait-time ignores range params** — Static seed; documented in
  `docs/backend-contracts/analytics.md`. Real backend should bucket by
  the supplied window without portal changes.
- **`jobsRun` KPI uses the truncated top-25 list** — A1 acceptable; A4
  may add a separate count endpoint or paginate.
- **`avgWait` KPI uses the same top-25 list** — same trade-off.

## Sign-off

- QA visual review: _pending_
- Architect review: _pending_
