// TF1 stub. The full presentational header (Breadcrumb, H1, +Add allocation
// CTA, StatCardRow) lands in phase TF1.
import type { Project } from "../schemas";

export type ProjectDetailHeaderProps = {
  project: Project;
};

export function ProjectDetailHeader(_props: ProjectDetailHeaderProps) {
  return null;
}
